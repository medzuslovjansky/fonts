{
  "name": "@interslavic/fonts-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "verify": "yarn workspaces foreach -A run verify"
  },
  "packageManager": "yarn@4.18.0"
}
